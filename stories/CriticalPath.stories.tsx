import React from "react";
import { CriticalPath } from "./CriticalPath";

export default {
  title: "CriticalPath",
  component: CriticalPath,
};

const Default = () => <CriticalPath />;

export { Default };
